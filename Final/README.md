# Отчет по Final_ZarsayevYevgeniy_IliasMukhatai_AssylkhanAkhmadi

## Введение

В данной работе была реализована и исследована операция префиксных сумм (inclusive prefix-sum, scan) для одномерного массива целых чисел. Основной целью работы являлось сравнение последовательного и параллельных подходов к вычислению prefix-sum на центральном процессоре и графическом процессоре, а также анализ их производительности и ускорения.

В рамках работы были реализованы три версии алгоритма:

1. Последовательная версия на CPU, использующая прямой проход по массиву;

2. Параллельная версия на CPU с применением технологии OpenMP, основанная на разбиении массива на независимые блоки с последующим добавлением смещений;

3. Параллельная версия на GPU с использованием CUDA, реализующая двухфазный алгоритм сканирования: локальный prefix-sum внутри блоков и последующий перенос сумм блоков ко всем элементам.


Префиксная сумма является фундаментальной операцией в параллельных алгоритмах и широко используется в сортировках, обработке данных, графических и численных вычислениях. Несмотря на кажущуюся последовательную природу, данная операция эффективно распараллеливается за счёт разбиения данных и многоэтапного вычисления частичных результатов.

# Блок-схема

![блок-схема](https://github.com/user-attachments/assets/d6e23cc5-c538-48e4-a48b-ef43a3c8f242)


# Результаты

![results](https://github.com/user-attachments/assets/ee207483-d90b-4cb1-987c-f2a97b77130b)


## Последовательная версия(CPU)

Последовательная реализация prefix-sum служила базовой эталонной версией для проверки корректности и оценки ускорения параллельных алгоритмов. Алгоритм имеет линейную сложность 
𝑂(𝑁) и минимальные накладные расходы, что делает его эффективным для малых размеров входных данных.

Из результатов измерений видно, что для небольших массивов последовательная версия демонстрирует наименьшее время выполнения. Однако при увеличении размера массива время работы растёт линейно, и отсутствие параллелизма становится основным ограничивающим фактором производительности.



## Параллельная версия CPU(OpenMP)

Параллельная версия на CPU была реализована с использованием OpenMP и основана на разбиении массива на части, обрабатываемые разными потоками, с последующим добавлением смещений между блоками.

Результаты показывают, что для средних и больших размеров данных данная версия может ускорять вычисления по сравнению с последовательной, однако эффект ускорения ограничен. При больших размерах массива наблюдается снижение эффективности, связанное с накладными расходами на синхронизацию потоков, неравномерную загрузку и ограниченную пропускную способность памяти.

Таким образом, OpenMP-версия демонстрирует преимущества параллелизма на CPU, но масштабируемость данного подхода ограничена архитектурными особенностями центрального процессора.


## Параллельная версия GPU(CUDA)

CUDA-реализация использует массовый параллелизм GPU и двухфазный алгоритм сканирования: сначала prefix-sum вычисляется внутри блоков, затем выполняется перенос сумм блоков ко всем элементам. Отдельно измерялось время выполнения CUDA-ядер и общее время, включающее копирование данных между CPU и GPU.

Результаты показывают, что чистое время выполнения GPU-ядер существенно меньше времени CPU-реализаций для больших массивов. Однако общее время выполнения (GPU total) значительно увеличивается за счёт накладных расходов на передачу данных между хостом и устройством.

Это демонстрирует, что использование GPU наиболее эффективно для задач с большими объёмами данных и минимальным числом копирований памяти, а также подчёркивает важность различия между временем вычислений и полным временем выполнения задачи.



# Вывод

В ходе работы были реализованы и сравнены последовательная, CPU-параллельная и GPU-параллельная версии алгоритма prefix-sum. Последовательная версия оказалась наиболее эффективной для малых размеров данных и использовалась как эталон корректности. Версия с OpenMP позволила частично ускорить вычисления за счёт параллелизма на CPU, однако её масштабируемость ограничена накладными расходами и архитектурой процессора. CUDA-реализация продемонстрировала наилучший потенциал производительности при больших размерах данных, но её эффективность существенно зависит от стоимости передачи данных между CPU и GPU.

Полученные результаты наглядно показывают, что выбор подхода к реализации prefix-sum должен учитывать размер входных данных и особенности целевой вычислительной архитектуры.
